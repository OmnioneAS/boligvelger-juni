// Re-export from the shared lib location.
// Both the editor and the public embed use this hook.
export { useActiveView } from '@/lib/useActiveView';
export type { UseActiveViewReturn } from '@/lib/useActiveView';
